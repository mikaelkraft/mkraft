#!/usr/bin/env sh
. "$(dirname "$0")/_/husky.sh"

# Run lint-staged (will execute configured formatters/tests on staged files)
npm run lint:staged
echo "Running fast test subset..."
npm run --silent test:fast || exit 1
